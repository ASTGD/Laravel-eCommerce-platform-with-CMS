# 07 Deployment

Production deployments must be isolated per installation:

- separate code checkout or artifact deployment
- separate `.env`
- separate database
- separate storage bucket or disk path
- separate worker processes
- separate credentials

Next completion point:

- add the production checklist, storage guidance, worker guidance, and release handoff steps during hardening
