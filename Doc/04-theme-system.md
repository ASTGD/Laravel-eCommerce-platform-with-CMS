# 04 Theme System

This document captures:

- token structure
- preset strategy
- render layers
- storefront theming rules
- section and block rendering contracts

Next completion point:

- complete after the default theme and preset resolution layer are verified end-to-end
